HTML Rendering Rules:

- When ask for a list of data on a known database table, return a full <table> structure.
- Include <thead> and <tbody>.
- If no records, show a table with "No records found".
- Do not nest or duplicate tags.
